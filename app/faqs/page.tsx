'use client';
import type {Metadata} from 'next';import {Container} from '@/components/Container';import {Plus} from 'lucide-react';import {useState} from 'react';
const faqs=[['What does BizNiti do?','BizNiti provides digital marketing and strategic services spanning websites, SEO, social media, PPC, content, email and fractional marketing leadership.'],['Do you work with businesses of different sizes?','Yes. The current BizNiti positioning describes its work as serving businesses of all sizes with strategies shaped around their needs and goals.'],['Can I start with one service?','Yes. A business can start with the capability that addresses its immediate need and connect additional services when there is a clear reason to do so.'],['What does the process look like?','The work begins with context and goals, moves into a focused strategy, then execution, measurement and refinement.'],['How do I get started?','Use the project brief to share what you are trying to change and BizNiti can take the conversation from there.']];
export default function FAQs(){const [open,setOpen]=useState<number|null>(null);return <main><section className="pb-20 pt-36"><Container><span className="tag">FAQs / useful answers</span><h1 className="display mt-14 max-w-6xl text-[clamp(4.2rem,9vw,9rem)] leading-[.76]">The answers,<br/><span className="text-[var(--blue)]">upfront.</span></h1></Container></section><section className="paper-grid pb-24"><Container><div className="rounded-[28px] border border-black/10 bg-white p-2">{faqs.map(([q,a],i)=><button type="button" key={q} onMouseEnter={()=>setOpen(i)} onMouseLeave={()=>setOpen(null)} onFocus={()=>setOpen(i)} onBlur={()=>setOpen(null)} onClick={()=>setOpen(open===i?null:i)} aria-expanded={open===i} className="group block w-full border-b border-black/10 p-5 text-left last:border-b-0 sm:p-7"><div className="flex items-start justify-between gap-6"><div className="flex gap-5"><span className="mono pt-1 text-[9px] text-black/30">0{i+1}</span><div><span className="text-xl font-semibold tracking-[-.03em] sm:text-2xl">{q}</span>{open===i&&<p className="mt-4 max-w-3xl text-sm leading-7 text-black/55">{a}</p>}</div></div><span className={`grid h-10 w-10 shrink-0 place-items-center rounded-full border border-black/10 transition ${open===i?'rotate-45 bg-[var(--blue)] text-white':''}`}><Plus size={17}/></span></div></button>)}</div></Container></section></main>}
